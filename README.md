# rock-paper-scissors

TODO: describe whole app architecture with frontend, backend, backend for frontend --> big picture and not the details,
those will come in the sub-repos, also add architecture diagram

also add screenshots for all app features to explain those

the docker-compose setup needs an .env file like this:

```
DB_PASSWORD=132
API_KEY=456
```

With the docker-compose setup, the client is available under http://127.0.0.1/

Next steps:

- decrease Docker images sizes:
- ![img.png](images/docker_images.png)